apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  # this makes the pv by itself
  resources:
    requests:
      storage: {{ .Values.mysql.storage }}